/*odoo.define('currency_rate_journal', function (require) {
    "use strict";

    var core = require('web.core');
    var CalendarView = require('web.CalendarView');
    var Widget = require('web.Widget');
    var _t = core._t;
    var _lt = core._lt;
    var qweb = core.qweb;

    local.HomePage = Widget.extend({
        template: 'HomePageTemplate';
        start: function() {
            console.log("Test home page loaded");
        },
    });

    core.action_registry.add('test.homepage', local.HomePage);

    instance.web.client_actions.add('test.homepage', local.HomePage);
    return local.HomePage;
}*/