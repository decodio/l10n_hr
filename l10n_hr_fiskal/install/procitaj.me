sudo apt-get install python-dev python-m2crypto libxmlsec1-dev

- otpakiraj tar, odi u folder,
pyton setup.py build
-> OpenSSL !

sudo python setup.py install 

openssl pkcs12 -in certificate.pfx -out certificate.cer -nodes

Troubleshooting
ValueError: RSA key format is not supported
  - this error could happen if your private key is not encrypted.
  Please check if your private key is encrypted.
  If it is not please encrypt it (''openssl rsa -in key.pem -des3 -out passkey.pem'')

